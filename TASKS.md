# E-Ticaret Platformu — Görev Listesi

Bkz. [CLAUDE.md](./CLAUDE.md) mimari kararlar ve teknik konvansiyonlar için. Her görev için dev loop: Planla → Implement et → Testleri güncelle/yaz → Dökümanları güncelle/yaz → Commit mesajı yaz ve review için sun → Onay al → Commit et → burada işaretle.

Görevler **MVP'ye ulaşana kadar yapılacak her şeyi** kapsayan fazlara ayrılmıştır. Fazlar sırayla yapılmak üzere tasarlandı (her faz bir öncekine bağımlı), ama bir fazın içindeki maddeler arasında da bağımlılık sırası var — madde sırasını değiştirmeden ilerlemek önerilir.

---

## Faz 1 — İskelet & Kimlik ✅ tamamlandı

- [x] **Solution iskeleti + Identity modülü (walking skeleton)** — Clean Architecture, modüler monolith, modül başına şema, CQRS (MediatR + EF Core yazma + Dapper okuma), outbox pattern. 9 modülün proje iskeleti kuruldu (derlenir, DI'a kayıtlı); Identity modülü uçtan uca tam (kayıt, giriş, JWT access+refresh token, GET /api/users/{id}). Commit: `5c5d0d2`. Repo: https://github.com/aygunbayirdev/ecommerce-platform

---

## Faz 2 — Çekirdek Alışveriş Döngüsü (MVP'nin omurgası) ✅ tamamlandı

Amaç: bu faz bitince bir müşteri uçtan uca alışveriş yapabilmeli (ürüne bak → sepete ekle → sipariş ver → öde). Sıralama bağımlılığa göre: Identity (adres) → Catalog (ne satılıyor) → Inventory (stok var mı) → Cart (sepete ekle) → Order (sipariş) → Payment (öde).

- [x] **Identity: Adres Defteri** — `Address`, `User` aggregate'inin child entity'si olarak modellendi (mutasyon metodları `internal`, sadece `User` üzerinden erişilebilir — "en fazla bir varsayılan adres" kuralı böylece aggregate root seviyesinde garanti altına alındı). 5 command/query (`AddAddressCommand`, `UpdateAddressCommand`, `DeleteAddressCommand`, `SetDefaultAddressCommand`, `GetAddressesByUserIdQuery`) + `AddressesController` (`/api/addresses`, UserId JWT claim'inden okunur). Yol boyunca iki bug bulunup düzeltildi: (1) EF Core, `Guid.CreateVersion7()` ile constructor'da üretilen id'leri olan yeni child entity'leri "Modified" sanıyordu (bkz. `ModelBuilderExtensions.ApplyClientGeneratedKeys`, artık tüm modüllerde uygulanıyor), (2) boş iskelet modüllerin `OutboxProcessor`'ı henüz var olmayan tabloları sorgulayıp sürekli hata veriyordu (ilgili modül gerçek entity'ler alana kadar kayıt kaldırıldı). 12/12 test geçiyor.
- [x] **Catalog modülü — referans veriler** — `Category` (self-referencing hiyerarşi + `CategoryAttributes` child collection), `Brand`, `ProductAttribute` (global tanım seti). `Category.AssignAttribute`/`RemoveAttribute` "aynı özellik iki kez atanamaz" kuralını aggregate root seviyesinde garanti eder (unique index DB'de de var). 11 command/query + `CategoriesController`/`BrandsController`/`ProductAttributesController` — GET endpoint'leri anonim (public katalog gezinme), POST/PUT/attribute-assign `[Authorize(Roles="Admin")]` (projede ilk kez rol bazlı yetkilendirme kullanıldı). 6/6 test geçiyor. Not: kullanıcıyı Admin yapan bir komut henüz yok, test için DB'de elle güncellendi.
- [x] **Catalog modülü — Product/Variant/Image** — `Product` aggregate root, `ProductVariant`/`ProductImage` child'ları, `ProductVariantAttributeValue` de `ProductVariant`'ın child'ı (3 seviyeli hiyerarşi, projede ilk kez). `AddProductVariantCommand` handler'ı her `ProductAttributeId`'nin ürünün kategorisine atanmış olduğunu doğruluyor (cross-aggregate kontrol) + SKU global unique. `ProductImage.AddImage` ilk görseli veya `isPrimary:true` isteneni otomatik primary yapıyor, öncekini unmark ediyor. Okuma tarafında ilk kez `PagedResult<T>` kullanıldı (`GetProductsByCategoryQuery`, Dapper `QueryMultipleAsync` ile count+page tek round-trip) ve çok tablolu Dapper composition (`GetProductByIdQuery` — product+variants+attribute değerleri+images, 4 result set). 5 command/query + `ProductsController` (GET'ler anonim, POST'lar Admin-only). Yol boyunca bir bug bulunup düzeltildi: Dapper'ın record materyalizasyonu SQL `SELECT` kolon sırasına göre pozisyonel eşleşiyor, DTO'nun constructor parametre sırasıyla SQL'in kolon sırası uyuşmayınca `InvalidOperationException` fırlatıyordu — SQL kolon sırası DTO ile hizalanarak düzeltildi. 14/14 test geçiyor. **Domain event kararı:** `ProductVariantCreatedDomainEvent` gibi bir event (Inventory'nin otomatik `StockItem` açması için) bilinçli olarak eklenmedi — henüz tüketecek bir handler yok, Inventory görevine gelindiğinde event+tüketici birlikte eklenip uçtan uca test edilecek.
- [x] **Inventory modülü** — `StockItem` (aggregate root: ProductVariantId unique, AvailableQuantity, ReservedQuantity) + `StockMovement` (child, audit log — şimdilik sadece `Inbound` tipi, `Reserved`/`Released`/`Committed` Order görevinde eklenecek). `CreateStockItemCommand` bilinçli olarak **idempotent** (var olan variant için Conflict değil, mevcut Id'yi döner — outbox'ın at-least-once teslimat garantisiyle uyumlu olsun diye). **Projenin ilk gerçek modüller arası domain event akışı burada devreye girdi:** `Catalog`, `ProductVariant.Create`'de `ProductVariantCreatedDomainEvent` raise ediyor (outbox artık Catalog'a da bağlı — `OutboxMessageConfiguration` `BuildingBlocks.Infrastructure`'a taşınarak paylaşımlı hale getirildi), `Catalog.Application`'daki `ProductVariantCreatedDomainEventHandler` (WMS deseni: kaynak modül kendi handler'ında hedef modülün command'ini çağırır) `Inventory.Application`'a proje referansıyla `CreateStockItemCommand`'ı gönderiyor. Uçtan uca doğrulandı: varyant oluştur → birkaç saniye içinde `inventory.stock_items`'da otomatik satır açılıyor. `StockItemsController` (`GET`/`POST .../increase`, ikisi de Admin-only — stok miktarı müşteriye açık değil). `Reserve`/`Release`/`Commit` rezervasyon metodları bilinçli olarak bu turda yok, Order modülü görevinde eklenecek. Commit öncesi modüller arası event zincirini ve iş kuralı bütünlüğünü denetleyen bir tur yapıldı — event tetikleme mekanizması eksiksiz bulundu, ayrıca `RemoveCategoryAttributeCommand`'ın bir kategorideki varyantlarca kullanılan bir attribute'un kaldırılmasına izin verdiği bir referential-integrity boşluğu bulunup düzeltildi (`IProductWriteRepository.IsAttributeUsedByAnyVariantInCategoryAsync` ile Conflict döner artık). 34/34 test geçiyor (Identity+Catalog+Inventory toplamı).
- [x] **Cart modülü** — `Cart` (aggregate root: UserId nullable — guest sepeti) + `CartItem` (child, ProductVariantId+Quantity, fiyat/isim **saklanmaz**). İki tasarım kararı netleşti: (1) **Misafir sepeti adresleme** — misafir için sepet kendi `CartId`'siyle adreslenir (`POST /api/carts` anonim yeni sepet açar, client id'yi saklar), giriş yapmış kullanıcı için sepet `UserId`'den otomatik resolve/oluşturulur (`GetOrCreateCartForUserCommand`, JWT claim, route'ta hiç görünmez — Identity `AddressesController`'ın `CurrentUserId` deseninin aynısı). Misafir sepetinin girişte kullanıcı sepetiyle birleşmesi ("merge on login") bilinçli olarak kapsam dışı bırakıldı. (2) **Projenin ilk cross-module READ composition'ı** — `CartItem` fiyat/isim saklamadığı için (sepette fiyat hep canlı olmalı, Order'ın snapshot ilkesiyle simetrik ama ters yönde), `GetCartByIdQueryHandler` ve `AddItemToCartCommandHandler` Catalog'un yeni `GetProductVariantSummariesQuery`'sini **doğrudan senkron `ISender.Send()` çağrısıyla** kullanıyor — Catalog→Inventory'deki event+outbox deseninden bilinçli farkı: bu bir okuma kompozisyonu ve immediate consistency gerektiriyor, event'in kabul ettiği eventual consistency burada uygun değil. `Cart.Application`, yeni bir proje referansıyla `Catalog.Application`'a bağlandı. `CartsController`'da guest uçları (`/api/carts`, `/api/carts/{cartId}/...`) anonim, kullanıcı uçları (`/api/carts/mine/...`) `[Authorize]`. Uçtan uca doğrulandı: guest akışı (oluştur→ekle→miktar güncelle→sil→geçersiz varyant 404), kullanıcı akışı (`/mine` ilk çağrıda otomatik boş sepet açıyor, sonraki çağrılarda aynı sepeti döndürüyor, token'sız 401). 10/10 yeni test (Cart) + 44/44 toplam test geçiyor.
- [x] **Order modülü** — `Order` (aggregate root: OrderNumber, UserId, Status, adres **snapshot**'ı düz kolonlar olarak — Identity.Addresses'e FK değil, o anki değerin kopyası) + `OrderItem` (child, ürün adı+SKU+fiyat **snapshot**'ı — Catalog'daki fiyat sonradan değişse bile geçmiş sipariş bozulmaz) + `OrderStatusHistory` (child, state machine: Created → PaymentPending → Paid → Preparing → Shipped → Delivered/Cancelled, her geçiş guard'lı — sıra dışı bir geçiş Conflict döner). Sadece giriş yapmış kullanıcılar sipariş verebilir (Cart'ın aksine misafir siparişi yok — adres defteri `User`'ın child'ı, misafir adres saklama altyapısı MVP kapsamı dışı).
  **Checkout orkestrasyonu — projenin ilk çok-modüllü yazma kompozisyonu:** `CreateOrderCommandHandler` (`Order.Application`, yeni proje referansları: `Cart.Application`, `Identity.Application`, `Inventory.Application`) `ISender` ile sırayla Cart'ın sepetini okur (zaten Catalog'la zenginleştirilmiş — ayrıca Catalog'a referans gerekmedi), Identity'den adresi okur, `Order`'ı oluşturup **kaydeder (commit point)**, sonra Inventory'den stok rezervasyonu ister. Rezervasyon senkron `ISender` çağrısıyla yapılıyor (Cart→Catalog'daki okuma kompozisyonu deseninin yazma tarafı — checkout'un başarılı/başarısız olduğu müşteriye hemen dönmeli). **Cross-module atomiklik, dağıtık transaction kurmadan, sıralamayla çözüldü:** rezervasyon başarısız olursa (yetersiz stok) zaten kaydedilmiş olan sipariş `Cancelled`'a çevrilir (nedeniyle) — böylece başarısızlık her zaman denetlenebilir bir iz bırakır (iptal edilmiş sipariş kaydı), asla "hayalet rezervasyon" gibi görünmez bir kaynak sızıntısına yol açmaz. Bu, Payment mikroservise ayrılınca gerçek saga/compensating-transaction ihtiyacının neden doğduğunu gösteren bir referans noktası. Inventory'ye bu iş için toplu (bulk, hepsi-ya-da-hiçbiri) `ReserveStockCommand`/`ReleaseStockCommand`/`CommitStockCommand` eklendi (`StockItem.Reserve`/`Release`/`Commit`, `StockMovementType`'a `Reserved`/`Released`/`Committed` eklendi).
  `mark-paid`/`mark-preparing`/`mark-shipped`/`mark-delivered` admin-only geçici uçlar olarak eklendi (Payment/Shipping henüz yokken durum makinesini uçtan uca test edilebilir kılmak için — ileride bu geçişleri Order'ın raise edeceği domain event'ler sürecek, tıpkı Catalog→Inventory gibi; `OrderReadyForPaymentDomainEvent` gibi bir event bu turda **bilinçli olarak eklenmedi**, Product/Variant görevindeki "tüketicisiz event ekleme" kararının aynısı — Payment görevine gelindiğinde event+tüketici birlikte eklenecek). Sipariş iptali sadece sipariş sahibi tarafından yapılabilir (`CancelMyOrderCommand`, ownership UserId eşleşmesiyle kontrol edilir), Shipped/Delivered sonrası iptale izin verilmiyor.
  Yol boyunca bir bug bulunup düzeltildi: `OrderNumber` ilk üretilen halinde `Guid.CreateVersion7()`'nin **ilk** 8 hex karakteri kullanılıyordu — bunlar UUIDv7'nin zaman damgası kısmı olduğu için birbirine yakın zamanlı siparişler aynı `OrderNumber`'ı üretip unique index'te çakışıyordu (uçtan uca doğrulama sırasında gerçek bir `DbUpdateException` ile yakalandı). Rastgele bitlerin olduğu **son** 8 karaktere geçilerek düzeltildi, regresyon testi eklendi. 14/14 yeni test (Order) + 65/65 toplam test geçiyor. Uçtan uca doğrulandı: mutlu yol (sepet→sipariş→stok rezerve→sepet boşalıyor), yetersiz stok (409, sipariş Cancelled olarak kayıtlı), sahiplik kontrolü (başka kullanıcının siparişini görme/iptal etme 404), tam durum zinciri (Paid→Preparing→Shipped→Delivered, sıra dışı çağrı Conflict).
- [x] **Payment modülü (monolith içinde, mock ödeme akışı)** — `Payment` (aggregate root: OrderId, Amount, Status) + `PaymentTransaction` (child, audit log — her deneme, `IdempotencyKey` unique). **Stripe test-mode entegrasyonu önce denendi, sonra vazgeçildi** — Stripe kayıt ekranında Türkiye desteklenen ülkeler arasında yok, kullanıcı ülke bilgisini yanlış beyan etmek istemedi (bkz. CLAUDE.md madde 9). Karar: şimdilik mock/test ödeme akışıyla devam, gerçek gateway (iyzico) **Faz 7**'de opsiyonel olarak eklenecek.
  `IPaymentGateway` (Identity'deki `IPasswordHasher`/`ITokenGenerator` ile aynı port/adapter deseni) tek soyutlama noktası — bugünkü implementasyon `MockPaymentGateway` (kart `"0000"` ile bitiyorsa red, aksi halde onay). `ProcessPaymentCommandHandler` (`Payment.Application`, yeni proje referansı: `Order.Application`) sipariş sahipliğini ve `PaymentPending` durumunu `GetOrderByIdQuery` ile doğruluyor, gateway'i çağırıyor, `Payment.Attempt` ile idempotency-key korumalı sonucu kaydediyor, başarılıysa var olan `MarkOrderAsPaidCommand`'ı çağırıyor (Order tarafında yeni kod gerekmedi). Bağımlılık **tek yönlü** (`Payment.Application` → `Order.Application`) — döngüsel referans riski, mock akışta Order'ın Payment'a bir "hazırlık" event'i göndermesine hiç gerek olmamasıyla ortadan kalktı.
  **Denetim sırasında bulunup düzeltilen bir eksiklik:** Order görevinde eklenen stok rezervasyonu (`ReserveStockCommand`) hiçbir zaman çözülmüyordu — ödenen siparişlerde stok sonsuza kadar `Reserved` kalıyordu. `MarkOrderAsPaidCommandHandler` artık `CommitStockCommand` çağırıyor (kalıcı düşüş), `CancelMyOrderCommandHandler` sadece `PaymentPending`'den iptalde `ReleaseStockCommand` çağırıyor (rezervasyon serbest kalıyor) — `Paid`/`Preparing`'den iptal (iade senaryosu) bu MVP'de ele alınmıyor, bilinçli bir sınır.
  10/10 yeni test (Payment) + Order'a 4 yeni handler testi (Commit/Release doğrulaması) + 79/79 toplam test geçiyor. Uçtan uca doğrulandı: başarılı ödeme → sipariş `Paid`, stok `Committed`; red edilen kart → 409, sipariş `PaymentPending`'de kalıyor; aynı idempotency key tekrar → Conflict; yeni key ile retry → başarılı; zaten `Paid` siparişe tekrar ödeme → Conflict; başka kullanıcının siparişine ödeme → 404; iptal edilen `PaymentPending` sipariş → stok `Available`'a geri dönüyor.

**Faz 2 bitiş kriteri:** bir müşteri register olup, ürünlere bakıp, sepete ekleyip, adres seçip sipariş verip, (mock) ödeme yapabiliyor olmalı — uçtan uca API testiyle doğrulanmalı (Identity Faz 1'de yapıldığı gibi curl/Postman ile).

---

## Faz 3 — Deneyim Zenginleştirme ✅ tamamlandı

Temel akış Faz 2 sonunda çalışıyor olacak; bu faz gerçekçiliği ve Trendyol-benzeri deneyimi tamamlıyor. Modüller arasında sıkı bağımlılık yok, sıra önemsiz.

- [x] **Promotion modülü** — `Coupon` (aggregate root: Code, DiscountType, DiscountValue, ValidFrom/To, UsageLimit, IsActive) + `CouponRedemption` (child, audit log — `PaymentTransaction`/`StockMovement` ile aynı rol, hiç silinmez, `ReleasedAtUtc` ile terminal duruma geçer). `CalculateDiscount` (saf, mutasyonsuz iş kuralı: aktiflik+tarih aralığı+kullanım limiti kontrolü, yüzde/sabit tutar hesaplama, indirim subtotal'ı aşamıyor) hem `Redeem`'in hem admin panelinin ileride kullanabileceği bir preview'un ortak temeli.
  **Mimari karar — Inventory'nin Reserve/Release deseninin kupona uygulanması:** Checkout zincirine üçüncü bir adım eklendi: `CreateOrderCommandHandler` stok rezervasyonundan sonra (kupon kodu verildiyse) `RedeemCouponCommand`'ı çağırıyor. Başarısız olursa (süresi geçmiş/limit dolmuş/geçersiz kod) hem az önce rezerve edilen stok **release** ediliyor hem sipariş **Cancelled**'a çevriliyor — zincirin neresinde başarısızlık olursa olsun denetlenebilir bir iz kalıyor. `CancelMyOrderCommandHandler`, `PaymentPending`'den iptalde `ReleaseCouponRedemptionCommand`'ı da çağırıyor (kupon kullanılmamışsa no-op) — `Coupon.ReleaseRedemption` redemption'ı silmiyor, `ReleasedAtUtc` ile işaretliyor (limit sayımına artık girmiyor, aynı kupon tekrar kullanılabiliyor).
  `Order`'a `CouponCode`/`DiscountAmount` snapshot kolonları eklendi, `Total = Max(0, Items toplamı - DiscountAmount)`. **Payment modülünde hiçbir değişiklik gerekmedi** — `ProcessPaymentCommandHandler` zaten `order.Total`'ı okuyup gateway'e gönderiyor, indirimli tutar otomatik doğru şekilde tahsil edildi (modüller arası kompozisyonun düzgün çalıştığının kanıtı). `Promotion.Application` → `Order.Application` referansı yok, tersine `Order.Application` → `Promotion.Application` (Cart/Identity/Inventory referanslarındaki desenin devamı, döngüsel referans riski yok).
  `CouponsController` (`POST /api/coupons`, `POST /api/coupons/{id}/deactivate`, `GET /api/coupons` — üçü de Admin-only, kupon kodları müşteriye açık gezinme verisi değil). Kupon önizleme (preview) endpoint'i bilinçli olarak eklenmedi — stok müsaitliğinin de sepette değil sadece checkout anında kontrol edilmesiyle tutarlı (CLAUDE.md madde 6).
  **Uçtan uca doğrulama sırasında bulunup düzeltilen bir bug:** `GetCouponsQuery`'nin Dapper okuma sorgusu `System.InvalidOperationException` fırlatıyordu — Postgres'in `COUNT(*)` alt sorgusu `bigint` (Int64) dönüyordu ama `CouponDto.UsedCount` `int` (Int32) olarak tanımlıydı, bu tip uyuşmazlığı Dapper'ın record constructor eşleştirmesini bozuyordu. SQL'de `COUNT(*)::int` cast'i eklenerek düzeltildi — Dapper'ın "SQL kolon tipi DTO alan tipiyle birebir eşleşmeli" gotcha'sının bir başka örneği (Product/Variant görevindeki kolon-sırası bug'ının kardeşi).
  12/12 yeni test (Promotion) + Order'a 7 yeni test (kupon senaryoları: happy path, redemption başarısız→stok release+iptal, `ApplyDiscount` domain testleri, cancel→coupon release) + 96/96 toplam test geçiyor. Uçtan uca doğrulandı: %10 kuponla sipariş → `Total` indirimli, ödeme indirimli tutarı tahsil ediyor; kullanım limiti dolu kuponla ikinci sipariş → 409, sipariş `Cancelled`, stok `Available`'a geri dönüyor; `PaymentPending` siparişi iptal → kupon redemption'ı `ReleasedAtUtc` ile işaretleniyor, aynı kupon tekrar kullanılabiliyor; süresi geçmiş kupon → 409; admin olmayan kullanıcı kupon oluşturamıyor (403).
- [x] **Review modülü** — `Review` (aggregate root, child entity'si yok: ProductId, UserId, OrderId, Rating, Comment, IsApproved). Projenin üçüncü "kaynak modül hedefin query/command'ini `ISender` ile çağırır" örneği, ama ilk kez **iki modülü aynı anda, tamamen okuma amaçlı bir doğrulama zinciri için** kullanıyor (Payment→Order tek modül, Order→{Cart,Identity,Inventory,Promotion} yazma orkestrasyonuydu).
  **"Satın alma doğrulaması" akışı:** `CreateReviewCommandHandler` (`Review.Application`, yeni proje referansları: `Order.Application`, `Catalog.Application`) önce `(ProductId, UserId)` unique kontrolüyle daha önce yorum yapılmış mı bakıyor, sonra `GetOrderByIdQuery` ile siparişi okuyup sahiplik + durumun `Paid`/`Preparing`/`Shipped`/`Delivered`'dan biri olduğunu (ödemesi tamamlanmamış/iptal edilmiş sipariş üzerinden yorum yapılamaz) doğruluyor, son olarak siparişin kalemlerini (`ProductVariantId` taşıyor, `ProductId` değil) Catalog'un **mevcut** `GetProductVariantSummariesQuery`'si ile `ProductId`'lerine çözüp `request.ProductId`'nin gerçekten bu siparişte satın alındığını kontrol ediyor.
  **Yeni bir Catalog query'si yerine mevcudun genişletilmesi:** `GetProductVariantSummariesQuery`'nin döndürdüğü `ProductVariantSummaryDto`'ya `ProductId` alanı **sona** eklendi (Dapper'ın SQL-kolon-sırası/DTO-parametre-sırası pozisyonel eşleştirme kuralı korunarak — Product/Variant görevindeki gotcha'nın tekrarı) — neredeyse birebir aynı bir "variant→product eşleştirme" query'si yazmak yerine tek satırlık bir SQL+DTO değişikliği tercih edildi, mevcut Cart tüketicileri (alanlara isimle erişiyorlar, pozisyonel değil) bozulmadı.
  Moderasyon basit bir boolean gate: `Review.Create` → `IsApproved: false`, `Review.Approve()` (zaten onaylıysa Conflict) tek admin eylemi — reddet/sil yok (TASKS.md'nin `IsApproved` alanına birebir karşılık, fazladan durum makinesi kurulmadı). `(ProductId, UserId)` üzerinde unique index — bir ürüne kullanıcı başına tek yorum hakkı (aynı zamanda "ürüne göre yorumlar" sorgusunun index'i, ayrı bir index gerekmedi).
  `ReviewsController`: `POST /api/reviews` (Authorize), `POST /api/reviews/{id}/approve` (Admin), `GET /api/reviews/by-product/{productId}` (anonim, sadece onaylılar), `GET /api/reviews/pending` (Admin moderasyon kuyruğu).
  13/13 yeni test (Review) + 109/109 toplam test geçiyor. Uçtan uca doğrulandı: ödemesi tamamlanmış bir siparişten yorum → başarılı ama onaylanana kadar herkese açık listede **görünmüyor**; admin onaylayınca **görünüyor**; aynı ürüne ikinci yorum denemesi → 409; `PaymentPending` sipariş üzerinden yorum denemesi → 409 ("OrderNotPurchased"); siparişte olmayan bir ürünle yorum denemesi → 409 ("ProductNotInOrder"); admin olmayan kullanıcı onaylayamıyor/moderasyon kuyruğunu göremiyor (403 x2); moderasyon kuyruğu onaylanmamış yorumu doğru gösteriyor.
- [x] **Shipping modülü** — `Shipment` (aggregate root: OrderId, Carrier, TrackingNumber, Status, FailureReason) + `ShipmentStatusHistory` (child, audit log — `OrderStatusHistory`'nin üçüncü tekrarı, StockItem/StockMovement ve Order/OrderStatusHistory'den sonra).
  **Order'ı sürükleme deseni — Payment'ın birebir tekrarı:** Order modülünde `MarkOrderAsShippedCommand`/`MarkOrderAsDeliveredCommand` zaten vardı (Order görevinden kalma admin-only stand-in uçlar, CLAUDE.md'de "Payment/Shipping modülleri gelince bu manuel endpoint'lerin yerini cross-module çağrılar alacak" notuyla bırakılmıştı). Bu görev onları gerçek çağrıcılarına kavuşturdu: `CreateShipmentCommandHandler` siparişin `Preparing` durumda olduğunu doğruluyor (Order'ın state machine'i `MarkAsShipped()`'i zaten sadece `Preparing`'den kabul ediyor), `Shipment.Create(...)` ile kargo kaydını `Shipped` durumunda açıp mevcut `MarkOrderAsShippedCommand`'ı çağırıyor; `MarkShipmentDeliveredCommandHandler` de aynı şekilde `MarkOrderAsDeliveredCommand`'ı çağırıyor. **Order tarafında hiç yeni kod yazılmadı** — Payment görevindeki "`MarkOrderAsPaidCommand`'ın gerçek çağrıcısına kavuşması" deseninin birebir devamı, projenin dördüncü "kaynak modül hedefin command'ini `ISender` ile çağırır" örneği.
  **`MarkDelivered`'ın Order'ın katı state machine'inden bilinçli sapması:** `Shipment.MarkDelivered()` hem `Shipped` hem `InTransit`'ten kabul ediliyor (Order'ın her geçişte tek bir öncül durum şart koştuğu modelin aksine) — gerçek kargo takibinde "yolda" güncellemesi garanti değil, bazı kargo firmaları doğrudan "Shipped"tan "Delivered"a bildirebiliyor. `MarkShipmentFailedCommand` (kayıp/iade senaryosu) sadece Shipment tarafını güncelliyor, Order'a dokunmuyor — Order'ın enum'unda kargo başarısızlığına karşılık gelen bir durum yok (`Cancelled` müşteri/sistem iptali, farklı bir kavram); bir iade/re-ship akışı bilinçli olarak bu MVP'nin kapsamı dışında bırakıldı.
  Bir siparişin en fazla bir kargo kaydı olabilir (`OrderId` üzerinde unique index — Payment'ın deseninin aynısı). `ShipmentsController`: `POST /api/shipments`, `POST /api/shipments/{id}/mark-in-transit`, `POST /api/shipments/{id}/mark-delivered`, `POST /api/shipments/{id}/mark-failed` (dördü de Admin-only), `GET /api/shipments/by-order/{orderId}` (Authorize — sahiplik kontrolü `PaymentsController`'ın `by-order` deseninin birebir aynısı, Shipping kendi UserId'sini bilmediği için önce Order'a soruyor).
  14/14 yeni test (Shipping) + 123/123 toplam test geçiyor. Uçtan uca doğrulandı: `Paid` siparişe doğrudan kargo oluşturma denemesi → 409 ("OrderNotReadyToShip"); `mark-preparing` sonrası kargo oluşturma → sipariş **otomatik** `Shipped`'e geçiyor; `mark-in-transit` → `mark-delivered` → sipariş **otomatik** `Delivered`'a geçiyor; aynı sipariş için ikinci kargo denemesi → 409; `GET by-order` tam durum geçmişini (`Shipped`→`InTransit`→`Delivered`) doğru sırayla dönüyor; sahibi olmayan/admin olmayan kullanıcı kargoyu göremiyor (404); admin olmayan kullanıcı kargo oluşturamıyor/güncelleyemiyor (403); `mark-failed` → `FailureReason` kaydediliyor, sipariş durumuna dokunulmuyor (bilinçli tasarım kararının doğrulanması).

**Faz 3 bitiş kriteri:** kupon uygulanabiliyor, satın alınan ürünlere doğrulamalı yorum yapılıp admin onayından sonra herkese görünüyor, siparişler kargoya verilip takip edilebiliyor ve teslimat otomatik olarak `Order.Status`'a yansıyor — hepsi uçtan uca doğrulandı.

---

## Faz 4 — Payment'ı Mikroserviste Çıkar (Strangler Fig) ✅ tamamlandı

Projenin asıl öğrenme amacı. Bkz. CLAUDE.md madde 2 ve 4 (Domain Event vs. Entegrasyon Eventi ayrımı). Kapsamlı, öğretici bir yazım için ayrıca bkz. masaüstündeki `Faz4-Payment-Mikroservis-Gecisi.md`.

- [x] **RabbitMQ altyapısı** — yeni `ECommercePlatform.BuildingBlocks.Messaging` projesi (`RabbitMQ.Client` paketini sadece Order ve Payment'ın ihtiyaç duyduğu yere taşımak için ayrı proje, diğer 7 modülün Infrastructure'ı buna hiç bağımlı değil). `RabbitMqOutboxPublisher<TDbContext>` (mevcut `OutboxProcessor<TDbContext>`'in polling/retry iskeletiyle aynı şekle sahip **paralel, ayrı** bir `BackgroundService` — Identity/Catalog'un çalışan in-process/MediatR akışına dokunulmadı, hedef RabbitMQ olan yeni bir sınıf eklendi) ve `RabbitMqConsumerBackgroundService<TMessage>` (generic tüketici, JSON deserialize edip verilen delegeyi çağırıyor, başarıda ack/hatada nack-no-requeue). Aynı `outbox_messages` tablosu/aynı "yaz + arka planda oku" güvencesi hem in-process (Identity/Catalog) hem RabbitMQ (Order/Payment) akışında paylaşılıyor — sadece dispatch hedefi farklı. `docker-compose.yml`'e `rabbitmq` servisi (`rabbitmq:4-management`, AMQP 5672 + yönetim arayüzü 15672) eklendi.
- [x] **Order → Payment entegrasyon eventi** — repo kökünde yeni `contracts/ECommercePlatform.IntegrationEvents` projesi (hem `backend/` hem `payment-service/` tarafından paylaşılacağı için repo kökünde, "hangi servise ait" izlenimi vermesin diye) — `OrderReadyForPaymentIntegrationEvent(OrderId, UserId, Amount)` ve `PaymentSucceededIntegrationEvent(OrderId, PaymentId, Amount)`, ikisi de sade `IDomainEvent` record'ları, başka hiçbir bağımlılığı yok. `Order.MarkReadyForPayment()` artık ilk event'i raise ediyor; `Order.Infrastructure` **ilk kez** outbox'a bağlandı (`order.outbox_messages` migration'ı) ve `OrderModule.cs`'e `AddRabbitMqOutboxPublisher<OrderDbContext>("order-ready-for-payment")` eklendi. Bu, **event-carried state transfer** deseni: Payment.Service, Order'a hiçbir zaman senkron sormuyor, ihtiyacı olan veriyi (UserId, Amount) event içinde taşıyıp kendi lokal kopyasında tutuyor.
- [x] **Payment'ı ayrı deploy birimine çıkar** — yeni `payment-service/` dizini: kendi bağımsız solution'ı (`PaymentService.slnx`), kendi `Dockerfile`'ı, kendi `Directory.Packages.props`/`Directory.Build.props`'u (repo köküyle aynı `backend/` ağacının **dışında**, kardeş bir dizin olduğu için CPM otomatik keşfi buraya ulaşamıyor — bu, servisin gerçekten bağımsız bir deploy birimi olduğunun somut kanıtı). `Payment.Domain`/`Application`/`Infrastructure` + testler `git mv` ile fiziksel olarak taşındı. `Payment.Domain`'e `UserId` alanı eklendi (`Create(orderId, userId, amount)`), `Attempt(...)` başarılı olduğunda `PaymentSucceededIntegrationEvent` raise ediyor. `Payment.Application`'da `ProcessPaymentCommandHandler` **tamamen yeniden yazıldı**: `ISender`/`Order.Application` referansı komple kalktı, artık lokal `Payment` kaydını okuyup (yoksa "Payments.OrderNotReady" — event henüz gelmemiş, eventual consistency'nin API'de görünen tek yansıması) `UserId` eşleşmesini kendi başına kontrol ediyor. Yeni `CreatePendingPaymentCommand`+handler (Catalog→Inventory'deki `CreateStockItemCommandHandler` ile birebir aynı idempotent-create deseni — RabbitMQ'nun at-least-once teslimatıyla uyumlu olsun diye) `OrderReadyForPaymentIntegrationEvent`'i tüketip lokal `Pending` kayıt açıyor. Yeni, kendi Postgres'i (`payment-postgres`, port **5434** — WMS 5432, monolith 5433, port çakışması önleme deseninin üçüncü tekrarı), taze `InitialCreate` migration'ı. Yeni `ECommercePlatform.PaymentService.Api` host projesi: kendi `Program.cs`'i, kendi JWT Bearer doğrulaması (Identity'ye hiç bağımlı değil — sadece aynı `Jwt:SigningKey` config değerini paylaşıyor, CLAUDE.md madde 8'in stateless JWT kararının tam olarak burada karşılığını bulduğu an), taşınan `PaymentsController` (`GetByOrderId`'deki ownership kontrolü artık `GetOrderByIdQuery`'ye değil, Payment'ın kendi `UserId` kopyasına bakıyor).
- [x] **Monolith'ten Payment modülünü kaldır** — `backend/ECommercePlatform.slnx`, `ECommercePlatform.Api.csproj`, `Program.cs`'ten Payment'a dair her şey (proje referansı, `AddPaymentModule()`, `PaymentsController.cs`) kaldırıldı. `OrderModule.cs`'e simetrik karşı taraf eklendi: `AddRabbitMqConsumer<PaymentSucceededIntegrationEvent>("payment-succeeded", ...)` — `PaymentSucceededIntegrationEvent` geldiğinde var olan `MarkOrderAsPaidCommand`'ı çağırıyor (Order tarafında yeni iş kuralı kodu gerekmedi, sadece çağrı yolu senkron `ISender`'dan RabbitMQ tüketicisine taşındı). Monolith artık Payment'ın hiçbir projesine (Domain/Application/Infrastructure) referans vermiyor — sadece paylaşılan `IntegrationEvents` sözleşme tiplerine.
- [x] **Uçtan uca doğrulama** — `docker compose up -d --build` ile tüm 5 servis (postgres, payment-postgres, rabbitmq, backend, payment-service) ayağa kalktı (bu adımda backend/payment-service'in Docker build context'i repo köküne genişletildi, çünkü her ikisi de artık kardeş dizinlerdeki `contracts/`'a referans veriyor — `context: ./backend` ile `../contracts` erişilemezdi). Tam akış doğrulandı: sipariş oluştur → birkaç saniye içinde **payment-postgres**'te doğru `UserId`/`Amount`'la `Pending` bir `payment.payments` satırı otomatik açılıyor (Order→outbox→RabbitMQ→Payment consumer zincirinin kanıtı) → Payment.Service'e **doğrudan** (monolith üzerinden değil, `POST http://localhost:8081/api/payments`) ödeme yapılıyor → birkaç saniye içinde monolithteki sipariş **otomatik** `Paid`'e geçiyor ve stok `Committed`'a düşüyor (Payment→outbox→RabbitMQ→Order consumer zincirinin kanıtı, `MarkOrderAsPaidCommandHandler`'ın Faz 2'den beri değişmeyen `CommitStockCommand` çağrısı tetiklendi). **Dayanıklılık senaryosu:** `payment-service` durdurulmuş haldeyken bir sipariş daha oluşturuldu, event `order-ready-for-payment` kuyruğunda 1 mesaj/0 tüketici olarak bekledi (`rabbitmqctl list_queues` ile doğrulandı); `payment-service` tekrar ayağa kaldırılınca kuyruk otomatik boşaldı (0 mesaj/1 tüketici) ve gecikmeli `Pending` kayıt doğru veriyle oluştu — senkron bir çağrının hiç sağlayamayacağı bir dayanıklılık özelliği, canlı gözlemlendi. Son adım: monolithin artık kullanılmayan `payment` şeması (`ecommerce-postgres`) `DROP SCHEMA payment CASCADE` ile temizlendi — strangler fig'in son adımı, eski kod yolu tamamen söküldü.

---

## Faz 5 — Frontend

- [x] **Frontend teknoloji kararları** — Tek Next.js projesi (App Router), route bazlı ayrım (`/admin` altında admin panel, geri kalanı public site — ayrı deploy biriminin bu ölçekte getirisi görülmedi). **Next.js** seçildi (WMS'in Vite+React'inden bilinçli bir sapma): public e-ticaret sitesinde ürün sayfalarının SEO/indekslenmesi gerçek bir ihtiyaç, App Router'ın Server Component'leri bunu native sağlıyor. **shadcn/ui her yerde** (WMS ile aynı, tutarlılık için). Kimlik doğrulama WMS'teki kanıtlanmış deseni birebir taşıyor: `zustand`+`persist` ile access/refresh token client-side, `axios` interceptor'ı 401'de tek seferlik refresh+retry yapıyor — herkese açık katalog sayfaları (ürün listeleme/detay/kategori) Server Component olarak backend'in anonim GET'lerine `fetch` ile SSR yapıyor (SEO kazanımının kaynağı), kişiselleştirilmiş sayfalar (sepet/checkout/hesabım/admin) Client Component olarak axios+zustand ile çalışıyor. İki backend origin'i env değişkeniyle ayrı ayrı konfigüre edildi (`NEXT_PUBLIC_API_URL` monolith, `NEXT_PUBLIC_PAYMENT_API_URL` payment-service — checkout'ta ödeme adımı **doğrudan** payment-service'e gidiyor, monolith üzerinden değil). Detaylı gerekçe için bkz. CLAUDE.md madde 11.
- [x] **Proje iskeleti** — `frontend/` (Next.js 16, App Router, TypeScript, Tailwind v4), shadcn/ui kurulumu (bu shadcn sürümü Radix yerine **Base UI** (`@base-ui/react`) kullanıyor — `asChild` yerine `render` prop ile polymorphism; native olmayan bir elemente (`Link`) render edilen `Button`/`DropdownMenuTrigger` gibi bileşenlerde `nativeButton={false}` gerekiyor, bu proje boyunca dikkat edilecek bir nokta). `lib/axios.ts` (iki client: monolith + payment-service, ortak auth-refresh interceptor — `refreshAccessToken` her zaman monolithin `/auth/refresh`'ine gidiyor çünkü sadece Identity token üretiyor/yeniliyor). `features/auth/store.ts` (zustand+persist, WMS'ten taşındı) + `lib/jwt.ts` (JWT payload decode — sadece UI amaçlı, imza doğrulaması yok, backend zaten doğruladı). `SiteHeader`/`SiteFooter`, kök `layout.tsx`'e `Providers` (react-query + sonner Toaster) bağlandı. `vitest`+`@testing-library/react` kurulumu (WMS ile aynı), auth store için 3 smoke test yazıldı. `.env.example`/`.env.local` (placeholder değil, localhost dev URL'leri — gizli bilgi içermiyor). Tarayıcıda doğrulandı (header render, cart/login linkleri doğru).
- [x] **Auth sayfaları** — `app/login/page.tsx` ve `app/register/page.tsx`, ikisi de Client Component (form state + mutation). `features/auth/api/useLogin.ts`/`useRegister.ts` (`@tanstack/react-query` `useMutation`, `apiClient` ile `/auth/login`/`/auth/register`'a POST). Register akışı: kayıt başarılı olunca backend sadece `{ id }` döndürdüğü için, aynı ekrandan hemen `useLogin` ile aynı bilgilerle otomatik giriş yapılıp `/`'e yönlendiriliyor — kullanıcı ikinci bir form doldurmuyor. `lib/errors.ts` (`getApiErrorMessage`) — backend'in `ResultExtensions.ToProblemDetails()`'i zaten `ProblemDetails.Detail`'e gösterime hazır Türkçe mesaj koyduğu için frontend'de ayrı bir code→mesaj eşleştirme tablosu yok, direkt `Detail` okunuyor. Backend'de bu checkpoint sırasında eksik olduğu ortaya çıkan bir parça eklendi: monolith (`backend/src/ECommercePlatform.Api/Program.cs`) ve payment-service (`payment-service/.../Program.cs`) hiçbirinde CORS middleware yoktu — frontend'in origin'i (`http://localhost:3000`) `appsettings.json`'daki `Cors:AllowedOrigins`'ten okunan bir policy ile açıldı. Tarayıcıda uçtan uca doğrulandı: register→otomatik login→header'da "Giriş Yap"'ın "Hesabım"a dönüşmesi, ayrı login akışı, ve yanlış şifreyle "E-posta veya şifre hatalı." hata mesajının doğru gösterilmesi. `lib/errors.ts` için 3 unit test eklendi (`errors.test.ts`).
- [x] **Public katalog** — üç sayfa, üçü de Server Component, backend'in anonim GET'lerine `lib/server-fetch.ts` (native `fetch` wrapper'ı, `lib/axios.ts`'ten bağımsız — o client-side/auth-aware, bu SSR-only) ile istek atıyor: `app/page.tsx` (ana sayfa — kategori navigasyonu + tüm ürünlerin grid'i), `app/category/[categoryId]/page.tsx` (kategoriye göre filtrelenmiş grid, bulunamayan id için `notFound()`), `app/products/[productId]/page.tsx` (görsel galerisi, açıklama, varyant listesi — SKU/fiyat, sepete ekleme yok, bu **Sepet** checkpoint'inde gelecek — ve onaylı yorumlar). Backend'de eksik çıkan bir parça: ana sayfa için "tüm ürünler" listesini döndürecek bir endpoint yoktu (`GetProductsByCategoryQuery` sadece zorunlu `categoryId` alıyordu) — `GetProductsQuery` olarak yeniden adlandırılıp `CategoryId` nullable yapıldı, `null` iken tüm ürünler dönüyor; aynı düzeltmeyle bu sorguya `is_active = true` filtresi de eklendi (daha önce pasif/taslak ürünler de public listede görünüyordu — ürün detay sayfası bu filtreyi bilerek yapmıyor, doğrudan linkle erişim hâlâ çalışsın diye). `components/catalog/` altında `ProductCard`/`ProductGrid`/`CategoryNav`/`ProductGallery` (thumbnail tıklamayla ana görsel değişen, tek `useState`'li Client Component — sayfanın geri kalanı Server Component) `/VariantList`/`ReviewList`/`StarRating`. Ürün görselleri admin'in gireceği keyfi URL'ler olacağı için `next/image`'ın remote-pattern allowlist'i pratik değil — düz `<img>` kullanıldı. `lib/format.ts` (`formatPrice`, `Intl.NumberFormat('tr-TR', ...)`) için 3 unit test eklendi. Test verisi olmadığı ve admin paneli henüz yokken (checkpoint 106/107) doğrulama için `testuser1@example.com` dev veritabanında doğrudan SQL ile Admin yapılıp o token'la birkaç kategori/ürün/varyant/görsel/yorum API üzerinden oluşturuldu (kalıcı kod değil, sadece test fixture) — tarayıcıda uçtan uca doğrulandı: ana sayfa grid'i, kategori filtreleme, ürün detayında galeri + varyant fiyatları + sadece onaylı yorumların (pending olan hariç) görünmesi, ve SSR'ın gerçekten çalıştığı `curl` ile ham HTML'de ürün adının JS çalışmadan görünmesiyle doğrulandı.
- [x] **Sepet** — backend'de hiç değişiklik gerekmedi, Cart modülü (Faz 2) zaten guest+authenticated akışının ikisini de destekliyordu. Guest sepetler cookie/session değil `CartId` ile adresleniyor (`POST /carts` → client id'yi saklıyor, sonraki her çağrıda `/carts/{id}/...`'a taşıyor) — bu id `features/cart/store.ts`'te (`useGuestCartStore`, auth store'daki zustand+persist deseninin aynısı) localStorage'da tutuluyor, ilk "Sepete Ekle"'ye kadar hiç oluşturulmuyor (boşta gezen ziyaretçi için boş sepet yaratılmasın diye). Giriş yapmış kullanıcı `/carts/mine/*`'a gidiyor; guest→user sepet birleştirme yok (backend'de böyle bir komut yok, kapsam dışı bırakıldı — girişten sonra `GetOrCreateCartForUserCommand` zaten kendi sepetini sıfırdan oluşturuyor). `features/cart/api/cartBasePath.ts` (`isAuthenticated` + `guestCartId`'den doğru path'i çıkaran saf fonksiyon, 3 unit testi var) `useCart`/`useAddCartItem`/`useUpdateCartItemQuantity`/`useRemoveCartItem` arasında paylaşılıyor, her mutasyon `['cart']` react-query key'ini invalidate ediyor. `app/products/[productId]/page.tsx`'teki salt-okunur `VariantList` kaldırılıp yerine `components/cart/AddToCartForm.tsx` kondu (varyant seçimi — dropdown değil buton/chip, Base UI'ın `Select`/`DropdownMenu` gibi popup tabanlı bileşenlerinde otomasyon aracında daha önce tıklama sorunu yaşandığı için bilinçli tercih — + adet + "Sepete Ekle", `sonner` toast'ıyla başarı/hata bildirimi). `components/cart/CartItemRow.tsx` (+/− adet butonları, satır toplamı, kaldır) ve `app/cart/page.tsx` (liste + genel toplam + boş sepet durumu) eklendi. `components/layout/site-header.tsx`'teki sepet ikonuna, sepetteki toplam adedi gösteren (0 iken gizlenen) bir `Badge` eklendi. Tarayıcıda uçtan uca doğrulandı: guest olarak ekleme (ilk eklemede otomatik `POST /carts`), adet artırma/azaltma, kaldırma (boş sepet durumuna dönüş dahil), header badge'in güncellenmesi, ve giriş yapmış kullanıcı için aynı akışın `/carts/mine` üzerinden (hiç guest cart id kullanılmadan) çalıştığı network isteklerinden teyit edildi.
- [x] **Adres defteri** — backend'de hiç değişiklik gerekmedi, Identity modülünün `AddressesController`'ı (Faz 2) zaten tam CRUD + varsayılan adres yönetimi sunuyordu. Kod okunurken iki domain-logic detayı doğrulandı (`User.cs`): `AddAddress` ilk eklenen adresi `isDefault` ne gönderilirse gönderilsin otomatik varsayılan yapıyor, `RemoveAddress` silinen adres varsayılansa kalan ilk adresi otomatik yeni varsayılan yapıyor — bu ikisi sayesinde create formunda `isDefault` alanına hiç gerek kalmadı (varsayılan her zaman ayrı "Varsayılan Yap" butonuyla, `PUT /addresses/{id}/default`, değiştiriliyor) ve silme sonrası "kimse varsayılan değil" durumunu client'ta ayrıca yönetmeye gerek kalmadı. `features/addresses/` (types + `useAddresses`/`useAddAddress`/`useUpdateAddress`/`useDeleteAddress`/`useSetDefaultAddress`, hepsi `['addresses']` invalidate ediyor — Sepet'teki guest/authenticated dallanması burada yok, path her zaman sabit `/addresses`) ve `components/addresses/` (`AddressFormDialog` — Add/Edit ortak, kendi `open` state'ini yönetiyor; `AddressCard` — "Varsayılan" rozeti, Düzenle/Varsayılan Yap/Sil aksiyonları, silme onayı `window.confirm()` ile, ikinci bir Dialog kurulmadı) eklendi. `app/account/addresses/page.tsx`, `'use client'`, `useAuthStore`'dan token kontrolüyle çıkış yapmış kullanıcıyı API'ye hiç istek atmadan "giriş yapmalısınız" mesajına yönlendiriyor (CLAUDE.md'nin middleware/proxy kullanmama kararıyla tutarlı client-side guard). Yeni bir unit test eklenmedi — Sepet'teki `cartBasePath.ts` gibi dallanma mantığı içeren bir birim burada yok. Tarayıcıda uçtan uca doğrulandı: çıkış yapmışken guard, boş durum, ilk adres eklenince rozetin otomatik görünmesi, ikinci adres eklenince rozetsiz kalması, "Varsayılan Yap" ile rozetin taşınması, düzenleme, silme (iptal ve onay), varsayılan adres silinince kalan adresin otomatik varsayılan olması, son adres silinince boş duruma dönülmesi, ve header'daki "Adreslerim" linkinin doğru rotaya gittiği.
- [x] **Checkout + ödeme** — backend'e dokunulmadı (bu checkpoint için de her şey zaten hazırdı), ama backend API sözleşmesi (`POST /api/orders`, `CreateOrderCommandHandler`'ın sıralamayla-atomiklik orkestrasyonu, `POST /api/payments`/`GET /api/payments/by-order/{id}`, mock gateway'in `"0000"` kuralı, idempotency key semantiği) iki paralel araştırma turuyla tam kod okunarak çıkarıldı. İki sayfa: `app/checkout/page.tsx` (adres seçimi — yeni `components/checkout/AddressSelector.tsx`, native radio input'lar, Base UI'lı bir RadioGroup değil; adres yoksa `AddressFormDialog` sayfadan ayrılmadan tetiklenebiliyor; kupon kodu — önizleme/doğrulama endpoint'i backend'de hiç yok, kod körlemesine sipariş oluşturma anında deneniyor, geçersizse tüm sipariş oluşturma isteği hata veriyor; "Sipariş Ver" → `POST /orders` → `/checkout/{orderId}`'e yönleniyor) ve `app/checkout/[orderId]/page.tsx` (ödeme + onay — `OrderReadyForPaymentIntegrationEvent`'in RabbitMQ üzerinden asenkron tüketilmesi yüzünden sipariş oluşturulduktan hemen sonra `payment-service`'te henüz `Payment` kaydı olmayabiliyor; yeni `usePayment` hook'u kayıt oluşana kadar `react-query`'nin `refetchInterval`'ıyla polling yapıp bu yarışı çözüyor — kayıt bulununca `components/checkout/PaymentForm.tsx` gösteriliyor, `status === 'Succeeded'` olunca doğrudan onay ekranına geçiliyor). `features/orders/` ve `features/payments/` (ikisi de cart/addresses'teki hook deseninin aynısı; `features/payments/api/`'deki hook'lar **`paymentApiClient`** kullanıyor, `apiClient` değil — checkout'un ödeme adımı monolith üzerinden değil doğrudan payment-service'e gidiyor, CLAUDE.md madde 11'in frontend'deki karşılığı). `components/checkout/OrderItemsList.tsx` normalize edilmiş bir satır tipiyle hem sepet hem sipariş item'larını aynı bileşenle gösteriyor (iki DTO şekli birbirine çok yakın olduğu için). **Bilinçli sınır:** tam sipariş takip sayfası bu checkpoint'in değil bir sonrakinin işi, `/checkout/[orderId]` sadece minimal bir onay ekranı gösteriyor.

  Doğrulama sırasında iki gerçek bulgu çıktı: (1) daha önceki checkpoint'lerde seed edilen katalog ürünlerinin hiçbirine stok eklenmemişti (Inventory checkpoint'i o sırada odak değildi) — ilk sipariş denemesi gerçek bir "Yetersiz stok." hatasıyla karşılaştı, admin `POST /api/stock-items/{variantId}/increase` ile düzeltildi (test fixture, kalıcı kod değil). (2) `usePayment`'ın `refetchInterval`'ı otomasyon tarayıcısında hiç tetiklenmedi — sebefi `document.hasFocus()`/`visibilityState` "hidden" dönmesiydi; react-query varsayılan olarak sekme arka plandayken polling'i durduruyor. Bu sadece bir test ortamı garipliği değil, gerçek bir UX açığıydı (kullanıcı ödeme hazırlanırken başka sekmeye geçerse takılı kalabilirdi) — `refetchIntervalInBackground: true` eklenerek düzeltildi. Tarayıcıda uçtan uca doğrulandı: girişsiz/sepet boşken guard'lar, adres ekleyip kuponsuz sipariş verme → ödeme hazırlanıyor ekranı → başarılı kartla ödeme → onay ekranı + header sepet badge'inin kaybolması, ayrı bir siparişte `...0000` ile biten kartla red + aynı formdan farklı kartla tekrar deneme, ve geçersiz kupon koduyla sipariş oluşturmanın backend'in Türkçe hata mesajıyla (`"Kupon kodu bulunamadı."`) engellenmesi. Yeni bir unit test eklenmedi (Adres Defteri'nde olduğu gibi, test edilecek saf/dallanan bir mantık birimi yok).
- [x] **Siparişlerim + takip** — backend'e dokunulmadı; checkout checkpoint'inde zaten çıkarılan Order API sözleşmesine ek olarak Shipping modülünün müşteriye açık bir `GET /api/shipments/by-order/{orderId}` uç noktası olduğu doğrulandı (`[Authorize]`, admin-only değil, sahiplik sipariş üzerinden kontrol ediliyor; henüz kargoya verilmemiş sipariş için `404 Shipments.NotFound` — bu bir hata değil, normal "henüz kargoya verilmedi" durumu, öyle ele alındı: `features/shipments/api/useShipment.ts`'in `queryFn`'i 404'ü yakalayıp `null` veri döndürüyor, hata state'ine düşürmüyor). `app/orders/page.tsx` (liste, sayfalama, boş durum) ve `app/orders/[orderId]/page.tsx` (detay: adres — order'ın kendi snapshot alanlarından —, **`components/checkout/OrderItemsList.tsx`'in aynen tekrar kullanılması**, `OrderStatusHistoryTimeline`, `ShipmentInfo`, ve `useOrder`'ın checkout checkpoint'inden **aynen tekrar kullanılması**). `POST /api/orders/{orderId}/cancel` için `CancelOrderDialog` (sebep zorunlu, `Textarea`) — "İptal Et" butonu sadece `Order.Cancel`'ın domain guard'ının izin verdiği dört durumda (`Created`/`PaymentPending`/`Paid`/`Preparing`) gösteriliyor, `lib/orderStatus.ts`'teki `isOrderCancellable` bu guard'ı birebir yansıtıyor (backend'deki guard değişirse burada da güncellenmesi gerektiği kodda not düşüldü). `lib/orderStatus.ts`/`lib/shipmentStatus.ts` (enum→Türkçe etiket saf fonksiyonları) için `format.ts` deseninde birer unit test dosyası eklendi.

  Doğrulama sırasında bulunan bir bug: `useMyOrders`/`useOrder`/`useShipment` başta `enabled` parametresi almıyordu — sayfa "giriş yapmalısınız" mesajını göstermeden önce hook'lar zaten çağrılıp 401 alıyordu, bu da `lib/axios.ts`'teki interceptor'ın `redirectToLogin()`'ini tetikleyip sayfanın kendi guard mesajını hiç göstermeden `/login`'e sert yönlendirme yapıyordu (adres defteri/sepet/checkout'taki `useAddresses`/`useCart` zaten bu deseni doğru uyguluyordu, burada eksik kalmıştı) — üçüne de `enabled` eklenip sayfalardan `isAuthenticated` geçirilerek düzeltildi. Tarayıcıda uçtan uca doğrulandı: girişsizken her iki sayfada da (liste + detay) doğru guard mesajı ve hiç API çağrısı atılmaması; sipariş listesinde durum/tutarların doğru görünmesi; bir siparişin detayında adres+ürünler+durum geçmişinin doğru gösterilmesi; henüz kargoya verilmemiş bir siparişte "Henüz kargoya verilmedi." (404'ün hata ekranına düşmemesi); admin uçlarıyla (`mark-preparing` + `POST /api/shipments` + `mark-in-transit`) bir sipariş kargoya verilip `ShipmentInfo`'nun taşıyıcı/takip no/durum geçmişini doğru gösterdiği; `PaymentPending` durumundaki taze bir siparişte "İptal Et"in çalışıp durumu `Cancelled`'a çevirdiği ve sebebin durum geçmişinde göründüğü; `Shipped` durumundaki bir siparişte "İptal Et"in **gösterilmediği**.
- [x] **Ürün yorumu** — bu checkpoint, Sepet/Adres Defteri/Checkout/Siparişlerim'in aksine **küçük ama gerçek bir backend değişikliği** gerektirdi. `CreateReviewCommand`'ın body'si `{ productId, orderId, rating, comment }` istiyor, ama `GET /api/orders/{orderId}`'nin döndürdüğü `OrderItemDto`'da sadece `productVariantId` vardı — `ProductVariantId → ProductId` çözümlemesi sadece `GetProductVariantSummariesQuery` ile mümkündü ve bu sorgu hiçbir public endpoint'ten erişilebilir değildi (sadece Cart'ın `GetCartByIdQueryHandler`'ı ve şimdi Review'ın kendi `CreateReviewCommandHandler`'ı `ISender` ile internal çağırıyordu). Düzeltme, yeni bir mimari desen eklemedi — Cart'ın zaten yaptığı cross-module read composition'ın Order'ın okuma tarafına genişletilmesiydi: `OrderItemDto`'ya geriye dönük uyumlu, varsayılanlı bir `Guid ProductId = default` eklendi (`Review.UnitTests`'teki mevcut bir test bu DTO'yu 6 parametreyle, `ProductId`'siz kuruyordu — varsayılan sayesinde değişmeden derlenmeye devam etti), `OrderReadRepository`'nin SQL'ine placeholder bir `NULL::uuid AS "ProductId"` eklendi (`order` şemasında böyle bir kolon yok, `catalog` şemasıyla SQL join de yapılmadı — modül sınırını ihlal ederdi), ve `GetOrderByIdQueryHandler`'a `ISender` bağımlılığı eklenip `GetProductVariantSummariesQuery` ile item'lar gerçek `ProductId`'yle zenginleştirildi (`with` expression'larıyla, kayıt tipleri olduğu için). `Order.Application`'a yeni bir proje referansı (`Catalog.Application`) gerekti — Cart.Application zaten aynı referansa sahipti, döngüsel bağımlılık çıkmadı. `GetCartByIdQueryHandler`'ın kendisi hiç test edilmediği için (aynı desen, karşılığı yok) `GetOrderByIdQueryHandler`'a da yeni bir backend testi eklenmedi, mevcut konvansiyonla tutarlı. 100/100 backend testi (mevcutlar + değişmeden geçen Review testi) yeşil kaldı.

  Frontend: `features/orders/types.ts`'e `productId` eklendi, `features/reviews/`'e (Public Katalog checkpoint'inden var olan `types.ts`/`getApprovedReviews.ts`'e ek olarak) `CreateReviewRequest` tipi + `useCreateReview.ts` (yazma, `apiClient`) eklendi. `components/reviews/RatingInput.tsx` (1-5 tıklanabilir yıldız — `components/catalog/StarRating.tsx`'in salt-okunur gösterim bileşenine dokunulmadı, ayrı, tek sorumluluklu bir bileşen) ve `ReviewFormDialog.tsx` (`AddressFormDialog`/`CancelOrderDialog`'daki Dialog deseni). Backend'de "zaten değerlendirdin mi" diye önceden sorulacak bir uç nokta yok — dialog submit-and-handle-409 yaklaşımını kullanıyor, `Reviews.AlreadyReviewed` gelirse `getApiErrorMessage` ile aynen gösteriliyor; başarıda kalıcı bir onay ekranı yok (her yeni yorum `IsApproved:false` ile moderasyon kuyruğuna giriyor, kullanıcı kendi bekleyen yorumunu görebileceği bir sorgu da yok), sadece tek seferlik bir toast. `app/orders/[orderId]/page.tsx`'e yeni bir "Ürünleri Değerlendir" bölümü eklendi — **sadece `order.status === 'Delivered'` iken görünüyor** (backend `Paid`/`Preparing`/`Shipped`/`Delivered`'ın hepsinden yorum kabul ediyor, ama TASKS.md bu checkpoint'i "teslim alınan siparişlerden" diye tanımladığı için frontend bilinçli olarak daha dar bir alt kümeyi gösteriyor), `order.items`'dan `productId`'ye göre tekilleştirilmiş liste (aynı üründen birden fazla varyant alınmışsa tek "Yorum Yap" gösterilsin diye). `components/checkout/OrderItemsList.tsx`'e dokunulmadı — salt-gösterim sorumluluğu korundu, yeni bölüm sayfada ayrı bir blok. Yeni bir frontend testi yok (test edilecek saf/dallanan bir birim yok).

  Tarayıcıda uçtan uca doğrulandı: `Delivered` olmayan bir siparişte bölümün hiç görünmemesi; admin uçlarıyla (`mark-preparing`/`mark-shipped`/`mark-delivered`, zaten var olan endpoint'ler) bir siparişi `Delivered`'a getirip bölümün görünmesi; yorum gönderme (4 yıldız + metin) → başarı toast'ı + dialog kapanması; aynı ürüne ikinci kez denemede `"Bu ürünü zaten değerlendirdiniz."` hatasının doğru gösterilmesi; ve **Faz 5'in ilk uçtan uca yorum döngüsü**: gönderilen yorum admin tarafından onaylanınca (`POST /api/reviews/{id}/approve`) ürünün public sayfasında (Public Katalog checkpoint'indeki `ReviewList`/`StarRating`) gerçekten göründüğü — Review modülünün okuma (Faz 5 checkpoint 4) ve yazma (bu checkpoint) taraflarının ilk kez birlikte, gerçek veriyle çalıştığının kanıtı.
- [x] **Admin panel — katalog yönetimi** — bu checkpoint TASKS.md'de "CRUD" diye tanımlandığı için önce kapsam netleştirildi: backend'in katalog tarafı o ana kadar neredeyse tamamen "sadece oluştur"du (Kategori/Marka'da isim+sıra değiştirme vardı ama silme/pasife alma yoktu, Ürün/Varyant/Görsel'de **hiç** güncelleme/silme/pasife alma, Stok'ta sadece "artır" — azaltma/toplu listeleme yoktu). Üç seçenek (minimal/dengeli/tam) sunuldu, **"dengeli"** seçildi: Ürün+Varyant için Deactivate/Reactivate, Ürün için görsel kaldırma, Stok için toplu listeleme eklendi; tam alan güncellemeleri (ürün adı/açıklama, varyant fiyat/SKU değiştirme) bilinçli olarak kapsam dışı bırakıldı.

  **Backend:** `Product.cs`'e `Coupon.Deactivate()`'in guard desenini (zaten hedef durumdaysa `Conflict`) birebir izleyen beş metod eklendi: `Deactivate`/`Reactivate`, `DeactivateVariant`/`ReactivateVariant` (varyant mutasyonu `Address`/`CartItem` deseninin üçüncü tekrarı olarak `Product` üzerinden, `ProductVariant`'a `internal Deactivate/Reactivate` eklenerek), `RemoveImage` (silinen görsel primary'yse ve başka görsel kaldıysa `DisplayOrder`'a göre ilk kalanı otomatik primary yapıyor — `AddImage`'ın invaryantını koruyor; son görsel de guard'sız silinebiliyor, kapsam bilinçli dar). Beşi için `Catalog.Application/Products/`'a `DeactivateCouponCommandHandler`'ın birebir aynı ince şekliyle beş command üçlüsü + `GetAllProductsForAdminQuery` (public `GetProductsQuery`'nin `is_active = true` filtresiz kopyası — pasife alınan bir ürün admin'den sonsuza dek kaybolmasın diye) eklendi. `ProductsController`'a altı yeni `[Authorize(Roles="Admin")]` uç (`/all` dahil — kod tabanının `/admin` önek konvansiyonu yerine `/pending` gibi zaten var olan düz isimlendirme tarzı izlendi).

  **Kabul edilen tasarım kararı:** `GetVariantSummariesAsync`'in SQL'i `ProductVariantSummaryDto.IsActive`'i sadece `pv.is_active`'ten okuyordu — bir ürünü pasife almak Cart'ın `AddItemToCartCommandHandler`'ının o varyantı reddetmesini hiç tetiklemiyordu (sadece public listeden kayboluyordu, doğrudan linkle hâlâ sepete eklenebiliyordu). Tek satırlık düzeltme (`(pv.is_active AND p.is_active) AS "IsActive"`) olmadan "Ürünü Pasife Al" butonu görsel bir ayrımdan öteye geçmezdi; uçtan uca doğrulandı (aşağıya bkz.).

  **Inventory — mimari bulgu:** `GetAllStockItemsQuery`'yi Cart/Order'daki cross-module read composition deseniyle (Catalog'un `GetProductVariantSummariesQuery`'sine `ISender` ile gidip stok satırlarını ürün adı/SKU ile zenginleştirme) Inventory.Application içine yazmaya çalışırken **gerçek bir dairesel bağımlılık** çıktı: `Catalog.Application` zaten Faz 2'den beri `Inventory.Application`'a referans veriyor (`ProductVariantCreatedDomainEvent` → `CreateStockItemCommand` outbox akışı için) — ters yönde bir referans `Catalog → Inventory → Catalog` döngüsü yaratıp derlenmiyordu. Çözüm, composition'ı **API katmanına taşımak** oldu: `GetAllStockItemsQuery`/Handler artık Inventory içinde hiçbir Catalog bağımlılığı olmadan ham `PagedResult<StockItemDto>` döndürüyor, zenginleştirme `StockItemsController.GetAll`'da yapılıyor (Api projesi zaten her iki modülün Infrastructure'ını, dolayısıyla Application'ını, transitif olarak referans veriyor — iki modülün Application katmanının birbirine referans vermesi gerekmeden composition'ı mümkün kılan tam da bu). Bu, "iki modül Application katmanında karşılıklı bağımlı olacaksa composition'ı composition-root'a (Api) taşı" kuralının ilk somut örneği — gelecekteki benzer durumlar için referans alınabilir.

  Yeni handler'lar için Catalog.UnitTests'e happy-path + Conflict + NotFound testleri eklendi (Deactivate/Reactivate ×2 entity, `RemoveImage`'ın primary-otomatik-yeniden-atama ve son-görsel-silme durumları dahil) — 19'dan 35'e çıktı, `GetAllStockItemsQueryHandler` ise ince enrichment handler'ları hiç test edilmediği (kurulu konvansiyon) için test edilmedi. Tüm backend suite (108 test) yeşil.

  **Frontend:** `/admin` altında ilk admin route'u — `app/admin/layout.tsx` (client-side guard: `useCurrentUser()?.role !== 'Admin'` ise mesaj, header'daki "Admin Panel" linki zaten Siparişlerim checkpoint'inden beri vardı), `AdminNav.tsx` (veri-güdümlü `NAV_SECTIONS`, bir sonraki checkpoint "Operasyon" bölümünü ekleyecek şekilde tasarlandı). `features/catalog/`'a `Brand`/`ProductAttribute`/`ProductVariantAttributeValueInput` tipleri + ~15 yeni authenticated hook (kategori/marka/özellik CRUD, kategori-özellik atama, admin ürün listesi/detayı, varyant/görsel ekleme-kaldırma, deactivate/reactivate çiftleri). Yeni `features/inventory/` (cart/orders'la aynı modül-başına-feature deseni). Dropdown'lar (kategori/marka/özellik seçimi) yine Base UI `Select` değil düz `<select>` — projenin tekrar tekrar uygulanmış otomasyon-tıklama kararının dördüncü tekrarı. `AddVariantDialog` sadece `useCategoryAttributes(product.categoryId)`'nin döndürdüğü, o kategoriye zaten atanmış özellikleri gösteriyor; kategori sayfasına bunun için gerekli bir `CategoryAttributesDialog` (atanmış özellikleri chip olarak gösterip ekleme/kaldırma) eklendi — planın bahsetmediği ama akışın çalışması için zorunlu olduğu implementasyon sırasında ortaya çıkan bir parça. 6 sayfa: `admin/{categories,brands,attributes,products,stock}` + `admin/products/[productId]` (tek sayfada başlık+durum+aktif/pasif, varyant tablosu+ekleme, görsel grid'i+ekleme). Yeni bir frontend testi yok (test edilecek saf/dallanan birim yok, kurulu konvansiyon).

  Tarayıcıda uçtan uca doğrulandı: admin olmayan/girişsiz `/admin/*` guard'ı; kategori oluşturma (üst kategoriyle) + özellik atama; özellik oluşturma; ürün oluşturma → detayına girip sadece atanmış özelliklerin seçilebildiği bir varyant ekleme → iki görsel ekleyip ikincinin primary'yi otomatik devralması → primary'yi kaldırıp kalanın otomatik primary olması; varyantı pasife alıp o varyantla sepete eklemenin `409 Carts.ProductVariantInactive` ile reddedilmesi; **ürünü pasife alıp** `GET /products/all`'da hâlâ görünürken public `GET /products`'ta kaybolduğunun ve o varyantla sepete eklemenin de aynı şekilde reddedildiğinin (yukarıdaki tasarım kararının kanıtı) doğrulanması; stok sayfasında ürün adı/SKU'nun doğru geldiği ve stok artırmanın satırı canlı güncellediği.
- [x] **Admin panel — sipariş/kargo/kupon yönetimi** — bu checkpoint dört ayrı modülü (Order, Shipping, Promotion, Review) aynı anda kapsadı. İki paralel araştırma turu backend'in bugünkü durumunu netleştirdi ve üç gerçek kapsam kararı `AskUserQuestion` ile kullanıcıya soruldu.

  **Order — en büyük gerçek eksik:** tüm durum geçiş uçları (`mark-paid`/`mark-preparing`/`mark-shipped`/`mark-delivered`) ve `GetOrderByIdQuery`'nin admin erişimi zaten vardı, ama **hiçbir admin "tüm siparişleri listele" sorgusu yoktu** — `GetOrdersByUserIdQuery` sadece `mine`'a kilitliydi, admin bir siparişi ID bilmeden bulamıyordu. `IOrderReadRepository.GetAllAsync` (opsiyonel `OrderStatus` filtreli, var olan `OrderSummaryDto`'yu yeniden kullanan — Katalog checkpoint'indeki `GetAllForAdminAsync` kararının aynısı) + `GetAllOrdersForAdminQuery` + `GET /api/orders/all` eklendi. Kullanıcı kararıyla (**"Evet, ekle"**) admin'e **zorla iptal** yetkisi de eklendi: `AdminCancelOrderCommand`, `CancelMyOrderCommandHandler`'ın (aynı stok/kupon serbest bırakma mantığıyla) sahiplik kontrolü olmayan ikizi — `POST /api/orders/{id}/admin-cancel`, müşterinin kendi `/cancel`'ından ayrı bir route.

  **Shipping — hiç backend değişikliği gerekmedi:** `CreateShipmentCommand`/`MarkShipmentInTransitCommand`/`MarkShipmentDeliveredCommand`/`MarkShipmentFailedCommand` + `GetShipmentByOrderIdQuery` zaten tamdı; kargo yönetimi sipariş detay sayfasından yürütülüyor (ürün detayının varyant/görsel yönettiği desenin aynısı), ayrı bir "tüm kargolar" listesine gerek görülmedi.

  **Promotion (Coupon) — kullanıcı kararıyla ("Dengeli") sadece Reactivate:** `Coupon.Reactivate()` (Product.Deactivate/Reactivate deseninin birebir aynısı) + `ReactivateCouponCommand` + `POST /api/coupons/{id}/reactivate`. Kupon şartları (kod/tutar/tarih/limit) bilinçli olarak düzenlenemez bırakıldı — Katalog checkpoint'indeki "tam alan güncellemesi yok" kararının ikinci uygulanışı.

  **Review — kullanıcı kararıyla ("Sil") reddetme + moderasyon zenginleştirmesi:** `IReviewWriteRepository.Remove` (projede bir aggregate root'un **ilk tam silinme örneği** — önceki silmeler hep child entity veya deactivate'ti) + `RejectReviewCommand` (guard'sız, "zaten reddedilmiş" diye bir durum yok) + `DELETE /api/reviews/{id}`. `ReviewDto` ham GUID taşıdığı için (`GetPendingReviewsQuery`'ye hiç dokunulmadan) `ReviewsController.GetPending`'e Inventory checkpoint'indeki composition-in-controller deseninin **üçüncü tekrarı** eklendi: `GetProductByIdQuery`/`GetUserByIdQuery`'yi (`ISender` ile) sayfadaki benzersiz id'ler için çağırıp yeni `ReviewAdminDto`'yla (ürün adı + yorumcu adı) birleştiriyor — `Review.Application`'a Catalog/Identity referansı eklenmedi.

  Gerçek mantık taşıyan yeni handler'lara test eklendi (`AdminCancelOrderCommandHandlerTests`, `ReactivateCouponCommandHandlerTests` + `Coupon.Reactivate()` domain testi, `RejectReviewCommandHandlerTests`) — ince passthrough `GetAllOrdersForAdminQueryHandler`'a (kurulu konvansiyon) eklenmedi. Backend suite 108'den 116'ya çıktı, hepsi yeşil.

  **Frontend:** `AdminNav`'a önceki checkpoint'te bilerek boş bırakılan "Operasyon" bölümü (Siparişler/Kuponlar/Yorumlar) eklendi. Sipariş detay sayfası `OrderStatusActions` (siparişin **o anki durumuna göre tek sonraki eylemi** gösteren buton — `VariantTable`'ın tek-durumdan-tek-eylem deseninin devamı: `PaymentPending`→"Ödendi İşaretle", `Paid`→"Hazırlanıyor İşaretle", `Preparing`→"Kargoya Ver") ile yürütülüyor; kargoya verme siparişi otomatik `Shipped`'e geçiriyor (backend'in `CreateShipmentCommandHandler`'ının zaten yaptığı cascade), sonrası `ShipmentActions` (Yolda/Teslim Edildi/Başarısız) devralıyor. Sayfa, müşteri tarafının `OrderItemsList`/`OrderStatusHistoryTimeline`/`ShipmentInfo` bileşenlerini **aynen yeniden kullanıyor**. Yeni `features/coupons/` modülü (`discountType` backend'de `JsonStringEnumConverter` kayıtlı olmadığı için sayısal `0|1` geliyor, frontend'de label lookup'la gösteriliyor). Yeni bir frontend testi yok — tek istisna `CouponFormDialog`'da bir React purity lint hatası bulunup düzeltildi (`Date.now()` doğrudan `useState` başlangıç değeri olarak render sırasında çağrılıyordu, lazy initializer + `Date.now()`'suz tarih hesaplamasına çevrildi).

  Tarayıcıda uçtan uca doğrulandı: admin sipariş listesinde **tüm müşterilerin** siparişlerinin (sadece admin'in kendisininkiler değil) durum filtresiyle göründüğü; bir siparişte tam akış — Ödendi İşaretle → Hazırlanıyor İşaretle → Kargoya Ver (firma+takip no) → siparişin otomatik Kargoya Verildi'ye geçmesi → Yolda İşaretle → Teslim Edildi İşaretle → siparişin Teslim Edildi'ye geçmesi; ayrı bir siparişte admin zorla iptali (sebep + durum geçmişinde görünmesi) ve **Kargoya Verildi bir siparişte iptal butonunun görünmediği**; kupon oluşturma + pasife alınmış bir kuponun Aktifleştir ile geri açılması; yorum moderasyon kuyruğunda ürün adı/yorumcu adının doğru geldiği (ve bilinmeyen kullanıcı için `"(bilinmiyor)"` fallback'inin çalıştığı), Onayla'nın yorumu **public ürün sayfasında gerçekten gösterdiği** (Ürün Yorumu checkpoint'inin okuma-yazma döngüsünün ikinci kez doğrulanması), Reddet'in yorumu kuyruktan kalıcı olarak kaldırdığı.
- [x] **Cilalama ve son doğrulama** — Faz 5'in kapanış checkpoint'i. Önceki 11 checkpoint her biri kendi golden path'ini doğruladığı için bu checkpoint'in asıl değeri (a) checkpoint'ler arası entegrasyon regresyonunu tek bir uçtan uca akışla yakalamak, (b) tüm site genelinde tutarlı olması gereken üç şeyi (responsive, boş durum, hata durumu) sistemli taramaktı.

  **Next.js route boundary'leri:** `find app -iname "error.tsx" -o -iname "not-found.tsx" -o -iname "loading.tsx"` boş döndü — hiçbiri yoktu. `not-found.tsx` (sitenin diliyle tutarlı 404) ve `error.tsx` (Client Component, `reset()` ile "Tekrar Dene") eklendi, ikisi de tarayıcıda doğrulandı.

  **`loading.tsx` denemesi — eklendi, gerçek bir regresyon bulundu, geri alındı:** SSR sayfalarının (`force-dynamic` işaretli `/`, `/category/[id]`, `/products/[id]`) fetch sırasında bir iskelet göstermesi için `app/loading.tsx` eklendi, ama tarayıcıda test edilince **sitenin tüm SSR sayfalarının kalıcı olarak yükleniyor durumunda kaldığı, hiç içerik göstermediği** bulundu — Next.js 16.3.0 + Turbopack'in `force-dynamic` sayfaları `loading.tsx`'in oluşturduğu Suspense sınırıyla sardığında streaming'in hiç çözülmediği bir framework seviyesi etkileşim (curl ile alınan ham HTML'de içerik nihayetinde stream'in içinde vardı, ama tarayıcı hiç patch'lemiyordu). Lokal `npm run dev`'de izole edilip doğrulandı (dosya kaldırılınca anında düzeliyor), bu yüzden `loading.tsx` **tamamen kaldırıldı** — bu kadar temel bir sayfanın (ana sayfa!) tamamen bozulmasına neden olan bir "cila" kabul edilemezdi. AGENTS.md'nin "bu Next.js sürümü training data'dan farklı olabilir" uyarısının somut bir örneği.

  **Responsive — kullanıcının "sitemiz responsive olsun mobilde de iyi görünsün" isteği üzerine genişletilen kapsam:** İki sistemik bulgu:
  1. `/admin` layout'u hiç responsive değildi (`AdminNav` sabit `w-48`, breakpoint'siz `flex` satırı) — `AdminNav.tsx` ve `app/admin/layout.tsx`, `md:` breakpoint'inde yan-yana/dikey liste, altında dikey-yığılmış/yatay-chip'ler şeklinde düzenlendi.
  2. Neredeyse her liste satırı bileşeni (`OrderList`, `CouponList`, `StockTable`, `ProductList`, `CategoryList`, `BrandList`, `PendingReviewList`, `OrderListItem` — 8 dosya) `flex items-center justify-between` kullanıyordu, `flex-wrap` **olmadan** — dar ekranda taşma riski. Hepsine `flex-wrap gap-y-2` eklendi (masaüstünde görsel etkisi yok, içerik zaten tek satıra sığıyor). `CartItemRow.tsx` daha ciddi bir örnekti (sabit genişlikli elemanlar toplam ~356px, 375px ekranda esnek metne yer kalmıyordu) — iki flex grubuna (görsel+metin / miktar+fiyat+sil) bölünüp `flex-wrap` ile dar ekranda otomatik iki satıra ayrılacak şekilde yeniden düzenlendi.

  Bu oturumun Browser paneli genelde ~570px'in altına inemedi (defalarca denendi), ama bir noktada gerçek 375px genişlik elde edilip doğrulandı: `/admin/orders`'da `document.querySelectorAll('body *')` ile tam sayfa taşma taraması **sıfır** taşan eleman buldu, nav dikeyden `w-full` yatay şeride döndü, `CartItemRow` iki gruba ayrıldı.

  **Boş/hata durumları zaten büyük ölçüde kapsanmıştı** — grep taraması SSR sayfaları dışında her `page.tsx`'in `isPending`/`isError` içerdiğini, her admin listesinde boş durum mesajı olduğunu doğruladı; ek iş sadece route boundary'leriydi.

  **Son uçtan uca golden path (yeni bir müşteri hesabıyla, tek kesintisiz akış):** kayıt → otomatik giriş → ürün sayfası (SSR) → sepete ekle → checkout'ta yeni adres ekleme → `DOCKERTEST` kuponuyla (%15) sipariş oluşturma (₺129,50 → ₺110,08, doğru hesaplandı) → mock ödeme → siparişlerim → (admin hesabına geçiş) sipariş listesinde **tüm müşterilerin** siparişleri arasında bulma → Ödendi İşaretle → Hazırlanıyor İşaretle → Kargoya Ver (MNG Kargo) → doğrudan Teslim Edildi İşaretle (ara "Yolda" adımı atlanarak, kargo durum makinesinin esnekliğinin kanıtı) → (müşteri hesabına dönüş) sipariş detayında "Ürünleri Değerlendir" bölümü → 5 yıldız + yorum gönderme → (admin'e dönüş) moderasyon kuyruğunda ürün adı/yorumcu adının doğru geldiği → Onayla → public ürün sayfasında yorumun gerçekten göründüğü (5.0/5, 1 değerlendirme). Yol boyunca `not-found.tsx`'in geçersiz bir ürün id'sinde doğru göründüğü de ayrıca doğrulandı. Hiçbir entegrasyon regresyonu bulunmadı (loading.tsx'in kendisi hariç, ki o zaten bu doğrulama sırasında yakalanıp düzeltildi).

  Faz 5 bu checkpoint'le kapandı — bir sonraki adım Faz 6 (CI/CD, test kapsamı genişletme, Hetzner deploy).

---

## Faz 6 — Üretime Hazırlık / Cilalama

- [ ] **CI/CD** — build + test pipeline (GitHub Actions), WMS'te de önerilip başlanmamıştı, bu projede en azından PR başına build+test çalıştırılması hedefleniyor.
- [ ] **Test kapsamını genişlet** — her modül için Identity'dekine benzer unit test kapsamı (handler testleri), en az Order ve Payment için entegrasyon testleri (Testcontainers ile gerçek Postgres).
- [ ] **Deploy** — Hetzner'deki kendi sunucumuza deploy edilecek (kesinleşti). Docker Compose'un yeterli olup olmadığına karar ver (Kubernetes şimdilik "gereksiz rabbit hole" olarak değerlendirildi). Faz 6 bitip MVP hazır olunca yayına alınır — Faz 7 (iyzico) bundan sonra, opsiyonel olarak ele alınır.

  **Ön adım tamamlandı (Faz 5 sırasında, ad-hoc):** `frontend/` de artık Docker Compose'da (`frontend` servisi, `output: 'standalone'`, üç aşamalı `frontend/Dockerfile`) — lokal dev-parity amaçlı, domain/TLS/reverse proxy gibi asıl prod deploy kararları hâlâ bu maddenin kapsamında. Gerçek bir mimari bulgu çıktı: `server-fetch.ts`'in SSR fetch'i (`NEXT_PUBLIC_API_URL` — build-time'da client bundle'ına gömülen, tarayıcının kullandığı değişken) frontend container'ının **içinde** çalıştığı için `localhost:8080` kendi container'ına işaret ediyordu, backend'e değil — ayrı, `NEXT_PUBLIC_` önekisiz (client bundle'ına hiç gömülmeyen) bir `INTERNAL_API_URL=http://backend:8080/api` (Docker service adı) eklenip `server-fetch.ts` önce onu, yoksa `NEXT_PUBLIC_API_URL`'i okuyacak şekilde güncellendi — `npm run dev` akışı değişmedi. Ayrıca `next build`'in `/` (parametre almayan tek SSR sayfa) için build-time prerender denediği ve backend image build sırasında ayakta olmadığı için sert hata verdiği bulundu — üç public SSR sayfasına da (`app/page.tsx`, `category/[categoryId]`, `products/[productId]`) `export const dynamic = 'force-dynamic'` eklenerek çözüldü (zaten CLAUDE.md madde 11'in "her zaman taze SSR" niyetiyle tutarlı). Host portu **3001**, 3000 değil — bu makinede WMS'in kendi frontend container'ı zaten 3000'i kullanıyor (Postgres'in 5433 gerekçesinin aynısı).

---

## Faz 7 — iyzico Entegrasyonu (opsiyonel, kullanıcı kendi eliyle yapacak)

Site Faz 6 sonunda mevcut mock ödeme akışıyla yayınlanacak. Bu faz **isteğe bağlı** ve **kullanıcı tarafından bizzat, öğrenme amaçlı** yapılacak — Claude burada sadece adım adım rehberlik edecek, kodu yazmayacak. Ne zaman başlanacağına dair bir tarih taahhüdü yok, kullanıcı müsait olduğunda gündeme gelecek.

- [ ] **`IyzicoPaymentGateway : IPaymentGateway`** — `Payment.Infrastructure/Gateways/`'e yeni bir sınıf, iyzico'nun resmi .NET SDK'sını kullanarak. `Payment.Domain`, `Payment.Application`, `PaymentsController`, testler — **hiçbiri değişmiyor** (bkz. CLAUDE.md madde 9, port/adapter deseninin tam olarak bunun için var olduğu nokta).
- [ ] **DI kaydı** — `PaymentModule.cs`'te tek satır: `MockPaymentGateway` yerine `IyzicoPaymentGateway`.
- [ ] **iyzico sandbox API key'leri** — `.env`/config'e eklenir (gitignored), `appsettings.json`'a placeholder.
- [ ] **Uçtan uca doğrulama** — iyzico sandbox'ında gerçek bir test kartıyla ödeme, mock akıştaki tüm senaryoların (başarılı/red/idempotency) sandbox'ta da çalıştığının doğrulanması.

---

## Notlar

- Bir fazın tamamlanması için tüm alt maddelerinin bitmesi şart değil — her madde kendi başına bağımsız bir "görev" (dev loop'un bir turu), tamamlandıkça işaretlenir.
- Faz sırası kesin değil, gerekirse (ör. frontend'i erken görmek istenirse Faz 5 öne çekilebilir) yeniden değerlendirilebilir — ama bir fazın içindeki madde sırası bağımlılık gerektirir.
- Faz 7, Faz 1-6'nın tamamlanmasına bağımlı değil önem sırası açısından ama pratikte site yayınlandıktan sonra ele alınacak (kullanıcının tercihi).
