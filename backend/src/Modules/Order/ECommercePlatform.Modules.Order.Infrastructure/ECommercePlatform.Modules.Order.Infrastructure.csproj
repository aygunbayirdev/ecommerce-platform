<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <ProjectReference Include="..\ECommercePlatform.Modules.Order.Application\ECommercePlatform.Modules.Order.Application.csproj" />
    <ProjectReference Include="..\..\..\BuildingBlocks\ECommercePlatform.BuildingBlocks.Infrastructure\ECommercePlatform.BuildingBlocks.Infrastructure.csproj" />
    <!-- RabbitMqOutboxPublisher<OrderDbContext> (publishing) + RabbitMqConsumerBackgroundService (consuming PaymentSucceeded) — see Faz 4. -->
    <ProjectReference Include="..\..\..\BuildingBlocks\ECommercePlatform.BuildingBlocks.Messaging\ECommercePlatform.BuildingBlocks.Messaging.csproj" />
    <ProjectReference Include="..\..\..\..\..\contracts\ECommercePlatform.IntegrationEvents\ECommercePlatform.IntegrationEvents.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!-- Lets Order.IntegrationTests construct OrderWriteRepository/OrderReadRepository directly
         (both internal — DI is how the real app wires them, but that path also drags in RabbitMQ/
         MediatR registration, which these Postgres-only integration tests deliberately avoid). -->
    <InternalsVisibleTo Include="ECommercePlatform.Modules.Order.IntegrationTests" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Dapper" />
    <PackageReference Include="EFCore.NamingConventions" />
    <PackageReference Include="Microsoft.EntityFrameworkCore" />
    <PackageReference Include="Microsoft.EntityFrameworkCore.Design">
      <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
      <PrivateAssets>all</PrivateAssets>
    </PackageReference>
    <PackageReference Include="Npgsql.EntityFrameworkCore.PostgreSQL" />
  </ItemGroup>

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

</Project>
