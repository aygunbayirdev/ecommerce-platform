# Monolith API (backend/) — auth, catalog, cart, orders, addresses, coupons, reviews, shipments.
NEXT_PUBLIC_API_URL=http://localhost:8080/api

# Payment.Service API (payment-service/) — its own origin, called directly by the client at checkout.
NEXT_PUBLIC_PAYMENT_API_URL=http://localhost:8081/api
