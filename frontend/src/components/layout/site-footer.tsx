export function SiteFooter() {
  return (
    <footer className="border-t py-6 text-center text-sm text-muted-foreground">
      <p>ECommercePlatform — portföy projesi. © {new Date().getFullYear()}</p>
    </footer>
  )
}
